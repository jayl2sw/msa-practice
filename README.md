# MSA

Spring Boot 실행

```sh 
mvn spring-boot:run -Dserver.port=9003'
./gradlew bootRun --args='--server.port=9003'
```



컴파일

```sh
mvn clean
./gradlew clean 
mvn compile package
./gradlew build
```

